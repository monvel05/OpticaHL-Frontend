import { ComponentFixture, TestBed } from '@angular/core/testing';
import { CalizdeformularioPage } from './calizdeformulario.page';

describe('CalizdeformularioPage', () => {
  let component: CalizdeformularioPage;
  let fixture: ComponentFixture<CalizdeformularioPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(CalizdeformularioPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
